import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jules.autopilottours',
  appName: 'AutoPilot Tours',
  webDir: 'dist'
};

export default config;
